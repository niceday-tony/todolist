# =============================================
# Environment Variables Template
# =============================================
# 이 파일을 .env로 복사하여 실제 값을 입력하세요
# cp .env.example .env
# =============================================

# GitHub Personal Access Token
# 생성 방법:
# 1. GitHub 로그인 → Settings → Developer settings
# 2. Personal access tokens → Tokens (classic)
# 3. Generate new token (classic) 클릭
# 4. 권한 선택:
#    - repo (Full control of private repositories)
#    - workflow (Update GitHub Action workflows)
#    - write:packages (Upload packages to GitHub Package Registry)
# 5. Generate token 클릭 후 토큰 복사
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# MySQL Database Configuration (Optional)
# Docker Compose 사용 시 기본값 사용
DB_HOST=localhost
DB_PORT=3306
DB_NAME=todolist
DB_USER=root
DB_PASSWORD=root

# Spring Boot Server Configuration (Optional)
SPRING_PROFILES_ACTIVE=local
SERVER_PORT=8080

# Vue.js Development Server (Optional)
VITE_PORT=5173
VITE_API_BASE_URL=http://localhost:8080/api

# MCP Server Configuration (Optional)
# Context7 API Key (선택사항 - 더 높은 rate limit을 원할 경우)
# https://context7.com/dashboard 에서 발급
CONTEXT7_API_KEY=