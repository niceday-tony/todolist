# 개인 태스크: Health Check API 구현
담당자: @minam

## 할당된 서브태스크
- [ ] HealthController 클래스 생성
- [ ] GET /api/health 엔드포인트 구현
- [ ] 데이터베이스 연결 상태 포함한 헬스체크 로직 구현

## API Contract (프론트엔드 팀과 공유)
```json
{
  "endpoint": "GET /api/health",
  "response": {
    "status": "UP",
    "database": "UP",
    "timestamp": "2024-09-20T10:00:00Z"
  },
  "httpStatus": 200
}
```

## 구현 상세

### 1. HealthController 클래스 생성
위치: `backend/src/main/java/com/study/todolist/controller/HealthController.java`

```java
package com.study.todolist.controller;

import com.study.todolist.dto.HealthCheckResponse;
import com.study.todolist.service.HealthCheckService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/health")
public class HealthController {

    private final HealthCheckService healthCheckService;

    @Autowired
    public HealthController(HealthCheckService healthCheckService) {
        this.healthCheckService = healthCheckService;
    }

    @GetMapping
    public ResponseEntity<HealthCheckResponse> healthCheck() {
        HealthCheckResponse response = healthCheckService.checkHealth();
        return ResponseEntity.ok(response);
    }
}
```

### 2. HealthCheckResponse DTO
위치: `backend/src/main/java/com/study/todolist/dto/HealthCheckResponse.java`

```java
package com.study.todolist.dto;

import com.fasterxml.jackson.annotation.JsonFormat;
import lombok.Builder;
import lombok.Data;

import java.time.LocalDateTime;

@Data
@Builder
public class HealthCheckResponse {
    private String status;
    private String database;

    @JsonFormat(pattern = "yyyy-MM-dd'T'HH:mm:ss'Z'")
    private LocalDateTime timestamp;
}
```

### 3. HealthCheckService 구현
위치: `backend/src/main/java/com/study/todolist/service/HealthCheckService.java`

```java
package com.study.todolist.service;

import com.study.todolist.dto.HealthCheckResponse;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import javax.sql.DataSource;
import java.sql.Connection;
import java.time.LocalDateTime;

@Service
public class HealthCheckService {

    private final DataSource dataSource;

    @Autowired
    public HealthCheckService(DataSource dataSource) {
        this.dataSource = dataSource;
    }

    public HealthCheckResponse checkHealth() {
        String databaseStatus = checkDatabaseConnection();

        return HealthCheckResponse.builder()
                .status("UP")
                .database(databaseStatus)
                .timestamp(LocalDateTime.now())
                .build();
    }

    private String checkDatabaseConnection() {
        try (Connection connection = dataSource.getConnection()) {
            if (connection.isValid(2)) {
                return "UP";
            }
        } catch (Exception e) {
            // 로그에 에러 기록
            return "DOWN";
        }
        return "DOWN";
    }
}
```

### 4. 테스트 클래스 작성
위치: `backend/src/test/java/com/study/todolist/controller/HealthControllerTest.java`

```java
package com.study.todolist.controller;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.web.servlet.MockMvc;

import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.*;

@SpringBootTest
@AutoConfigureMockMvc
public class HealthControllerTest {

    @Autowired
    private MockMvc mockMvc;

    @Test
    public void healthCheckShouldReturnOk() throws Exception {
        mockMvc.perform(get("/api/health"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.status").value("UP"))
                .andExpect(jsonPath("$.database").exists())
                .andExpect(jsonPath("$.timestamp").exists());
    }
}
```

## 테스트 방법
```bash
# API 테스트
curl http://localhost:8080/api/health

# 예상 응답
{
  "status": "UP",
  "database": "UP",
  "timestamp": "2024-09-20T10:00:00Z"
}
```

## 완료 기준
- [ ] /api/health 엔드포인트가 200 OK 응답을 반환함
- [ ] 응답에 status, database, timestamp 필드가 포함됨
- [ ] 데이터베이스 연결 상태가 정확히 표시됨
- [ ] 단위 테스트가 통과함

## 협업 필요
- @tony: 데이터베이스가 실행 중이어야 "UP" 상태 확인 가능
- @martin: Spring Boot 설정이 완료되어야 구현 가능
- @preah: API Contract에 따른 프론트엔드 Mock 구현