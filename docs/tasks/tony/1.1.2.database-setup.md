# 개인 태스크: 데이터베이스 초기 설정
담당자: @tony

## 할당된 서브태스크
- [ ] Docker Compose로 MySQL 컨테이너 실행 확인
- [ ] 데이터베이스 스키마 초기화 스크립트 작성
- [ ] JPA 엔티티 기본 설정 검증
- [ ] MySQL 연결 설정 확인 (with @martin)

## 구현 상세

### 1. Docker Compose로 MySQL 실행 확인
```bash
# docker-compose.yml 확인 (이미 존재)
docker-compose up -d mysql
docker ps # MySQL 컨테이너 실행 상태 확인
```

### 2. 스키마 초기화 스크립트
위치: `backend/src/main/resources/schema.sql`
```sql
CREATE DATABASE IF NOT EXISTS todolist;
USE todolist;

-- 기본 테이블 생성
CREATE TABLE IF NOT EXISTS users (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    email VARCHAR(255) NOT NULL UNIQUE,
    name VARCHAR(100),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE IF NOT EXISTS todos (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT,
    title VARCHAR(255) NOT NULL,
    completed BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    completed_at TIMESTAMP NULL,
    FOREIGN KEY (user_id) REFERENCES users(id)
);

-- 바질 화분 테이블 (Story 1.4를 위한 준비)
CREATE TABLE IF NOT EXISTS basil_pots (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT UNIQUE,
    level INT DEFAULT 1,
    experience_points INT DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

### 3. JPA 엔티티 설정 검증
- User, Todo, BasilPot 엔티티 클래스 확인
- @Entity, @Table 어노테이션 검증
- 관계 매핑 설정 확인

### 4. application.yml 설정 확인
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/todolist
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
```

## 의존성
- Docker Desktop 설치 및 실행
- MySQL 8.0 Docker 이미지

## 완료 기준
- [ ] MySQL 컨테이너가 정상적으로 실행됨
- [ ] 데이터베이스 'todolist'가 생성됨
- [ ] 모든 테이블이 생성됨
- [ ] Spring Boot 애플리케이션이 데이터베이스에 연결됨

## 협업 필요
- @martin: Spring Boot 프로젝트의 데이터베이스 설정 검증
- @minam: Health API에서 데이터베이스 연결 상태 확인 로직